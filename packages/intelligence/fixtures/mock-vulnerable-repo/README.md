# Deliberately vulnerable Mistral City fixture

This repository exists only to test the intelligence layer. It contains explicit insecure examples corresponding to the local OWASP WSTG 50 source-evidence probes. It is not a runnable service and must never be deployed.
