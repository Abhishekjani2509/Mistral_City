import "../src/auth";

// Expected red-team baseline: the OWASP WSTG 50 harness should detect all
// deliberately insecure indicators in this fixture before a repair exercise.
export const expectedDetections = 50;
